import { FFIndicatorDirective } from './ff-indicator.directive';

describe('FfIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FFIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
