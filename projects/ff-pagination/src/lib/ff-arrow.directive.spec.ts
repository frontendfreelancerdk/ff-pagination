import { FfArrowDirective } from './ff-arrow.directive';

describe('FfArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new FfArrowDirective();
    expect(directive).toBeTruthy();
  });
});
